/*
 * _104_led.c
 *
 *  Created on: Nov 30, 2024
 *      Author: vermi
 */

#ifndef SRC_C_
#define SRC_C_

#include <_104_led.h>

static void turn_on(Led* this, Button* button)
{
	TURN_ON_LED;

	if (this->last_button == false && button->is_pressed(button) == true)
	{
		this->operate = turn_off_led;
	}

	this->last_button = button->is_pressed(button);
}

static void turn_off(Led* this, Button* button)
{
	TURN_OFF_LED;

	if (this->last_button == false && button->is_pressed(button) == true)
	{
		this->operate = turn_on_led;
	}

	this->last_button = button->is_pressed(button);
}

Led* new_led(GPIO_TypeDef* GPIOx, uint16_t GPIO_Pin, Button* button)
{

}


#endif /* SRC_C_ */
